# open a project file in ~/.claude/ideas/projects (fzf picker)
local selected=$(find ~/.claude/ideas/projects -maxdepth 2 -type f -name '*.md' 2>/dev/null | sort | fzf --prompt="project> ")

if [ -n "$selected" ]; then
  ${EDITOR:-vim} "$selected"
fi
