# editor
alias e="$EDITOR"

# cd
alias ..='cd ..'

# ls
alias ls="ls -F"
alias l="ls -lAh"
alias ll="ls -l"
alias la='ls -A'

# tmux
alias tmux="tmux -2"
alias tmuxkill="tmux kill-session"

# others
alias gdf="cd ~/.dotfiles"
