# editor
alias e=nvim

# cd
alias ..='cd ..'

# ls
alias ls="ls -F"
alias l="ls -lAh"
alias ll="ls -l"
alias la='ls -A'

# dotfiles
alias mdf="cd ~/.dotfiles && nvim ~/.dotfiles"

# tmux
alias tmux="tmux -2"
alias tmuxkill="tmux kill-session"
